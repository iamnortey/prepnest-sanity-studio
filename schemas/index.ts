import lesson from './lesson'
import question from './question'

// Note: subject and topic schemas should already exist in your Sanity project
// Import them here if you have local copies

export const schemaTypes = [
  lesson,
  question,
]
